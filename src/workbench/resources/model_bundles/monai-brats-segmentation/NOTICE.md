# Third-party notice / 第三方声明

## English

This unmodified TorchScript artifact comes from the MONAI
`brats_mri_segmentation` bundle at revision
`370f7f9d062745fbac445e7fe6d6616d35df04ec`. OpenMedVisionX adds an integrity
record and adapters but does not claim ownership of the model. The pinned
bundle license is Apache-2.0. BraTS training-data terms remain separate; no
BraTS patient data is redistributed here.

## 中文

此未修改的 TorchScript 资产来自 MONAI `brats_mri_segmentation` bundle 的固定
版本 `370f7f9d062745fbac445e7fe6d6616d35df04ec`。OpenMedVisionX 仅增加完整性
记录和适配层，不主张模型所有权。固定版本的 bundle 采用 Apache-2.0 许可证；
BraTS 训练数据条款独立适用，本目录不再分发任何 BraTS 患者数据。
